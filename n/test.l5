(require sysmod.l5)

;;test l5
(defun inc (x)
  (+ x 1 1))

(defun max (a b)
  (if (> a b)
    (a)
    (b)))

(defun fib (x)
  (if (> x 2)
      (+ (fib (- x 1))
	 (fib (- x 2)))
    (1)))

(defun print (message)
  (stream-output message system-ostream))

(print (fib 6))
