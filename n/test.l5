;;;l5 test
(require n0.code)

(defun inc (x)
  (+ x 1 1))

(defun max (a b)
  (if (> a b)
    (a)
    (b)))

(defun fib (x)
  (if (> x 2)
      (+ (fib (- x 1))
	 (fib (- x 2)))
    (1)))

;(print (fib 8))
(test)