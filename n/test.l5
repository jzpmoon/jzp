;;test l5
(defun inc (x)
  (+ x 1 1))

(defun max (a b)
  (if (> a b)
    (a)
    (b)))

(defun fib (x)
  (if (> x 2)
      (+ (fib (- x 1))
	 (fib (- x 2)))
    (1)))

;(inc (+ 1 1))

(stream-output (fib 6) system-ostream)
